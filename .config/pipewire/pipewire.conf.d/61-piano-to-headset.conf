context.modules = [
  { name = libpipewire-module-loopback
    args = {
      node.description = "Piano to Headset"
      media.name       = "Piano to Headset"
      audio.position   = [ FL FR ]
      capture.props = {
        node.name      = "piano_to_headset_capture"
        media.class    = Stream/Input/Audio
        target.object  = "alsa_input.pci-0000_6b_00.6.analog-stereo"
        audio.position = [ FL FR ]
        node.passive   = true
      }
      playback.props = {
        node.name      = "piano_to_headset_output"
        media.class    = Stream/Output/Audio
        target.object  = "alsa_output.usb-SteelSeries_Arctis_Pro_Wireless-00.stereo-game"
        audio.position = [ FL FR ]
      }
    }
  }
]
